class Data {
  String url;
  List<dynamic> content = [];
  String htmlContent;
  String title;
  String id;
  String description;
  Data(this.url, this.content, this.htmlContent, this.title, this.id,
      this.description);
}
